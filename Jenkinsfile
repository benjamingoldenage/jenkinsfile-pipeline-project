pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Clarusway_Way to Reinvent Yourself. good afternoon"
                sh 'echo using shell within Jenkinsfile'
                echo 'not using shell in the Jenkinsfile. benjamin. david'
            }
        }
    }
}
